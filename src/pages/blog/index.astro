---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { getCollection } from "astro:content";

const posts = (await getCollection("blog")).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
---

<BaseLayout
  title="Blog"
  description="The RexBunny Services blog — SEO strategies, GEO tactics, AEO guides, web performance tips, and AI search insights from a marketing agency built for 2026."
>
  <section class="pt-24">
    <div class="mx-auto max-w-4xl px-4 py-20 sm:px-6 lg:px-8">
      <h1 class="mb-4 text-4xl font-extrabold text-gray-900 sm:text-5xl">Blog</h1>
      <p class="mb-12 text-lg text-gray-500">
        GEO strategies, SEO tactics, and web performance guides.
      </p>
      <div class="space-y-6">
        {
          posts.map((post) => (
            <article class="group rounded-xl border border-gray-200 bg-white p-6 transition-all hover:border-gray-300">
              <div class="mb-2 flex items-center gap-2 text-xs text-gray-400">
                <time>{post.data.pubDate.toLocaleDateString("en-US", { year: "numeric", month: "long", day: "numeric" })}</time>
                <span>Â·</span>
                <span>{post.data.author}</span>
              </div>
              <h2 class="mb-2 text-xl font-bold text-gray-900 group-hover:text-brand-600">
                <a href={`/blog/${post.id}`}>{post.data.title}</a>
              </h2>
              <p class="mb-3 text-sm text-gray-500">{post.data.description}</p>
              <div class="flex flex-wrap gap-2">
                {post.data.tags.map((tag) => (
                  <span class="rounded bg-gray-100 px-2 py-1 text-xs text-gray-500">{tag}</span>
                ))}
              </div>
            </article>
          ))
        }
        {
          posts.length === 0 && (
            <p class="text-gray-400">No blog posts yet. Check back soon!</p>
          )
        }
      </div>
    </div>
  </section>
</BaseLayout>
